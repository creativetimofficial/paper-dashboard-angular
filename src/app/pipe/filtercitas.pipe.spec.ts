import { FiltercitasPipe } from './filtercitas.pipe';

describe('FiltercitasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercitasPipe();
    expect(pipe).toBeTruthy();
  });
});
